/**
 * @file postgres.constants.ts
 * @description Token de inyección del pool de conexiones PostgreSQL.
 */

/** Símbolo de inyección del pool PostgreSQL compartido. */
export const POSTGRES_POOL = Symbol("POSTGRES_POOL");
